Alerts

- Create a simple modal for alerts instead of using the built-in alert

Themes

- Rename theme from classic to light
- Themes should default to match the system setting and allow the user to pick from system, light or dark.

Onboarding

- Create an way to help a new user understand how to use the sections of the app that they'll be able to stay engaged with. It should help
- First time users choose between
  - Create their first task and chore wheel OR
  - Load demo tasks and chore wheel and take them to their chore wheel
  - Learn how to enable more features
- Skeptics
  - Understand why this tool is helpful:
    - Helps keep you engaged with the things you want to do

UX

- Changing views should always scroll to the top
- Language Improvements
  - Priority and urgency can be a little confusing
- In wide desktop view, task filters should be in sidebar on left of tasks
- Add option to show/hide hidden tasks

TaskWheel

- The two options are confusing
  - Static
    - Each tasks leave the wheel until you complete or restart the wheel
  - Dynamic
    - Tasks matching the wheel's categories are added to the wheel when they're created or restart (for cyclic tasks).

Data

Add ability to import/export task data from json file

Tips

Add a tip jar

Scheduling

- Recurring tasks
  - should set to Midnight on the day of
  - should be able to be set for a specific day of the week or month
- Add completion range. Complete tasks within a day, week, month or whenever
  - This should replace the urgency options on the form.
  - Tasks without a due data are due 'whenever' (4)
  - Tasks with a due date default to being due on the day of, but user can select within a week and within a month options.
  - Helper text should be available to explain how this maps to urgency

Goals (new feature)

- To replace priority, tasks can optionally be assigned to goals
- Goals can be enabled in the settings menu
- Goals behave similar to categories, so build for reuse
- Priorities are set on goals instead of tasks to reduce overload
- A tasks priority is accessed through it's goals
- A task can contribute to more than one goal
-

Note Creation

- Optional steps should be collaped into a single group by default

Task List View

- Sort category filters alphabetically;
